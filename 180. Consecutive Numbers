-- Write your PostgreSQL query statement below
select DISTINCT num AS ConsecutiveNums
from logs l1
 where l1.num =(
    select l2.num
    from logs l2
    where l2.id = l1.id+1
)
AND l1.num =(
    select l3.num
    from logs l3
    where l3.id = l1.id+2
);
